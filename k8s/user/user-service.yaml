apiVersion: v1

kind: Service

metadata:
  name: user-service

spec:
  selector:
    app: user-deployment

  ports:
    - name: "http"
      port: 3001
      targetPort: 3001
    - name: grpc
      port: 50051
      targetPort: 50051